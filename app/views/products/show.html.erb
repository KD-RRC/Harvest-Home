<div class="product-show">
  <div class="breadcrumb">
    <%= link_to "Home", root_path %> &rsaquo;
    <%= link_to "Products", products_path %> &rsaquo;
    <%= @product.name %>
  </div>

  <div class="product-detail">
    <div class="product-detail-image">
      <% if @product.image.attached? %>
        <%= image_tag @product.image, alt: @product.name, class: "product-detail-img" %>
      <% else %>
        <div class="product-img-placeholder">
          <span>🌿</span>
        </div>
      <% end %>
    </div>

    <div class="product-detail-info">
      <h1><%= @product.name %></h1>

      <p class="product-categories">
        <% @product.categories.each do |category| %>
          <span class="category-tag"><%= category.name %></span>
        <% end %>
      </p>

      <p class="product-detail-price">
        <% if @product.current_price %>
          <%= number_to_currency(@product.current_price.amount) %>
        <% else %>
          Price not available
        <% end %>
      </p>

      <div class="product-detail-description">
        <h3>About this product</h3>
        <p><%= @product.description %></p>
      </div>

      <p class="product-sku">SKU: <%= @product.sku %></p>

      <p class="product-stock">
        <% if @product.stock_quantity <= 0 %>
          <span class="out-of-stock">✗ Out of Stock</span>
        <% elsif @product.stock_quantity <= 5 %>
          <span style="color: #e67e22; font-family: Arial, sans-serif;">⚠ Low Stock — only <%= @product.stock_quantity %> left!</span>
        <% else %>
          <span class="in-stock">✓ In Stock (<%= @product.stock_quantity %> available)</span>
        <% end %>
      </p>

      <% if @product.stock_quantity > 0 %>
        <%= button_to "Add to Cart", add_to_cart_path(@product), method: :post, class: "btn-primary" %>
      <% end %>

      <%= link_to "← Back to Products", products_path, class: "btn-secondary" %>
    </div>
  </div>
</div>